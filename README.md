# Jingyang Zhang — Research Portfolio

Static personal website based on the supplied preview.html.

## Publish with GitHub Pages

Publish the main branch from the repository root. The entry point is `index.html`; keep all six images in `assets/`. No build step is required.

## Update

Edit `index.html` and commit the changes. Keep asset filenames and capitalization unchanged.

The original CV PDF was not supplied, so those links were replaced with ORCID and Google Scholar.
